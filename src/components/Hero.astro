<section class="hero" id="inicio">
  <div class="hero-content">
    <h2>Calidad y Variedad en Cada Grano</h2>
    <p>El mejor arroz chino al mejor precio</p>
    <a class="hero-btn" href="/pageMenu">Ver Menú</a>
  </div>
</section>

<style>
  /* Hero */
  .hero {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 160px 20px;
    text-align: center;
    background-image: url('/images/fondo.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.75);
    z-index: 0;
  }

  /* Contenido */
  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-content h2 {
    color: #ffffff;
    font-size: 4rem;
    margin-bottom: 2rem;
  }

  .hero-content p {
    color: #cbd5e1;
    margin-bottom: 1.5rem;
    margin-bottom: 2rem;
  }

  /* Botón */
  .hero-btn {
    display: inline-block;
    background: #ffcc00;
    color: #0b0101;
    padding: 0.65rem 1.5rem;
    font-weight: 700;
    font-size: 1rem;
    border-radius: 8px;
    text-decoration: none;
    transition: background 0.2s ease;
  }

  .hero-btn:hover {
    background: #f0bb00;
  }

  /* responsive */
  @media (max-width: 768px) {
    .hero-content h2 {
      font-size: 2.4rem;
    }
  }
</style>